const numbers = [1, 6, 11, 10, 55, 54, 22, 2, 18, 99, 16]
for (let i = 0; i < numbers.length; i++) {
  if (numbers[i] % 2 === 0)
    console.log(numbers[i])
}

const rainbow = ['red', 'orange', 'yellow', 'blue', 'indigo', 'violet']
for (let i = rainbow.length - 1; i >= 0; i--) {
console.log(rainbow[i])
}

